/**
 * 後楽園ホールの寸法（概算, 単位: メートル）。
 *
 * 実測図面ではなく「プロレス開催時の見え方を体感できる」ことを目的とした近似値。
 * 座席表(2D)と3Dシーンはどちらもこの定数だけを参照するので、
 * ここを直せば両方の見た目が同時に変わる。
 *
 * 座標系: 右手系 Y-up。リング中央が原点。
 *   +X = 東, -X = 西, +Z = 南, -Z = 北, +Y = 上
 */

/** リングマット（1辺）とエプロン（マットの外周部分）。 */
export const RING = {
  /** マットの1辺の半分。20ft リング相当。 */
  matHalf: 3.05,
  /** エプロン込みの1辺の半分。 */
  apronHalf: 3.9,
  /** 床からマット面までの高さ。 */
  matY: 1.2,
  /** マット面からコーナーポスト頂点までの高さ。 */
  postHeight: 2.2,
  /** ロープ3本のマット面からの高さ。 */
  ropeHeights: [0.45, 0.9, 1.35],
} as const;

/** ホール本体の広さ。壁・天井の位置に使う。 */
export const HALL = {
  halfX: 20,
  halfZ: 21,
  ceilingY: 10,
  /** 2階席（バルコニー）の床の基準高さ。 */
  balconyBaseY: 2.6,
} as const;

/** 着席時の目線の高さ（座面ではなく床からの高さ）。 */
export const EYE_HEIGHT = 1.15;

/** カメラが初期状態で向く点（リング中央のやや上）。 */
export const RING_FOCUS: readonly [number, number, number] = [0, RING.matY + 1.0, 0];
