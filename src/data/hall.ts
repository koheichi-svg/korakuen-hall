/**
 * 後楽園ホール（プロレス開催時）の寸法。単位はメートル。
 *
 * 座席の平面配置は公式の座席表PDFから起こしていて（seat-layout.generated.ts）、
 * このファイルが受け持つのは高さ方向と会場の外形。どちらも公式サイトの
 * 座席からの見え方の写真に合わせた近似値。
 *
 * 座標系: 右手系 Y-up。リング中央が原点。+X=東 / +Z=南 / +Y=上。
 */

/** リング。プロレス用20フィートリング相当。 */
export const RING = {
  /** マット（キャンバス）の1辺の半分。 */
  matHalf: 3.05,
  /** エプロン込みの1辺の半分（ロープの外側はごく狭い）。 */
  apronHalf: 3.22,
  /** 床からマット面までの高さ。 */
  matY: 1.1,
  /** マット面からコーナーポスト頂点まで。 */
  postHeight: 1.62,
  /** ロープ3本のマット面からの高さ。 */
  ropeHeights: [0.45, 0.93, 1.41],
} as const;

/**
 * リングと最前列の間。写真だと、エプロンの周りに黒いマットが敷かれ、
 * その外周にシルバーの低い柵（バリケード）が立っている。
 * リングサイド席はこの柵の外側に来るように、座席表の位置から少し後ろへ下げる。
 */
export const RINGSIDE = {
  /** 黒いマットの縁までの距離。リングが北寄りなので北だけ狭い。 */
  mat: { N: 4.37, S: 5.04, E: 5.04, W: 5.04 },
  /** 柵のリング中央からの距離。 */
  barrier: { N: 4.27, S: 4.92, E: 4.92, W: 4.92 },
  barrierHeight: 0.9,
  /**
   * 柵から最前列までの距離と、列の間隔。
   * リングサイドの列は座席表の位置ではなく、柵からこの間隔で並べ直す
   * （マットと柵に場所を取られるので、最後列がスタンドにぶつからない範囲で詰める）。
   */
  frontGap: 0.45,
  pitch: 0.85,
} as const;

/**
 * ステージ席（東・西）が乗るステージ。
 * ひな壇と違って3列とも同じ高さで、高さは北側スタンドのC列に合わせる
 * （実際の高さは座席データ側で北側C列から取るので、ここには列名だけ持つ）。
 */
export const STAGE = { matchNorthRow: 'C' } as const;

/**
 * 会場の外形。リングが中央ではなく北寄りにあるため、南北で非対称。
 * 値は座席の広がり（x: -11.4〜11.7 / z: -14.3〜20.7）に余白を足したもの。
 */
export const HALL = {
  minX: -14.0,
  maxX: 14.0,
  minZ: -17.5,
  maxZ: 23.5,
  ceilingY: 9.6,
  /** 東西の壁にある2階ギャラリーの下端。 */
  galleryY: 5.4,
} as const;

/**
 * 東西の壁の上部にある2階バルコニー席。
 * 公式の座席表PDFには載っていないので、寸法も座席の並びも写真から起こした近似値。
 * 低い手すり壁のすぐ内側に1列。1階の客席をかなり上から見下ろす高さにある。
 */
export const BALCONY = {
  /** 床の高さ。1階の客席をだいぶ上から見下ろす位置にある。 */
  floorY: 6.6,
  /** リング側の縁（手すり壁の内面）と、奥の壁までの距離。 */
  innerX: 11.9,
  outerX: 14.0,
  /** 開口の南北の範囲。 */
  minZ: -6.4,
  maxZ: 6.4,
  /** 手すり壁の高さ（前列に座ってリングが見える高さに収めてある）。 */
  parapetHeight: 0.65,
  /** 開口の上端。ここから上は下がり天井。 */
  soffitY: 9.2,
} as const;

/**
 * スタンド（ひな壇）の傾斜。side ごとに「最前列の高さ」と「奥行き1mあたりの上がり」を持つ。
 * 最前列の位置は座席データから求めるので、ここには持たせない。
 * balcony だけは床が2階なので base が高い（奥の段は座席がないので slope は形だけ）。
 */
export const RAKE = {
  north: { base: 0.4, slope: 0.5 },
  south: { base: 0.55, slope: 0.48 },
  east: { base: 0.35, slope: 0.44 },
  west: { base: 0.35, slope: 0.44 },
  balcony: { base: BALCONY.floorY, slope: 0.56 },
} as const;

/** 着席時の目線の高さ（その席の床面からの高さ）。 */
export const EYE_HEIGHT = 1.15;

/** カメラが初期状態で向く点（リング中央、レスラーの高さあたり）。 */
export const RING_FOCUS: readonly [number, number, number] = [0, RING.matY + 1.0, 0];
